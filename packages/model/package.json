{
  "name": "@mondrian-framework/model",
  "version": "2.0.30",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "files": [
    "build"
  ],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "coverage": "vitest run --coverage",
    "prerelease": "tsc -p tsconfig.build.json"
  },
  "peerDependencies": {
    "@mondrian-framework/utils": "^2.0.30"
  },
  "dependencies": {
    "fast-check": "^3.16.0",
    "jsonwebtoken": "^9.0.2",
    "bignumber.js": "^9.1.2"
  },
  "devDependencies": {
    "@fast-check/vitest": "^0.1.0",
    "@vitest/coverage-v8": "^0.34.6",
    "@types/jsonwebtoken": "^9.0.6"
  }
}
