name: CI
on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      GIT_USER: twinlogix
      GIT_PASS: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - name: Docs deploy
        run: npm ci && npm run deploy-docs
